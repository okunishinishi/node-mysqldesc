/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 * @author {{pkg.author.name}}
 * @license {{pkg.license}}
 */

"use strict";

/** @lends module:mysqldesc */
var mysqldesc = require('./mysqldesc');

module.exports = mysqldesc;